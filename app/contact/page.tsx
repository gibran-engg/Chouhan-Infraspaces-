import type { Metadata } from "next";
import ContactPage from "./ContactForm";

export const metadata: Metadata = {
  title: "Contact",
  description:
    "Book a consultation with Chouhan Infraspaces. Tell us about your project and we'll respond within one business day.",
};

export default ContactPage;
